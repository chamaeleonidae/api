Chameleon's REST API allows you to interact with information in Chameleon to integrate it with your own systems. It's meant to be flexible and adapt to your own use cases.

Requests look like this: `curl -H "X-Account-Secret: ACCOUNT_SECRET" https://api.chameleon.io/...` -- see the pages below for more info

 - [Authentication](/concepts/authentication)
 - [Pagination](/concepts/pagination)
 - [Errors](/concepts/errors)
 - [Rate limiting](/concepts/rate-limiting)

------

> Use `https://api.chameleon.io` or `https://api.trychameleon.com` for all requests to the Chameleon API (either will work but use `chameleon.io` for new projects.)

Currently, our REST API supports the following endpoints:

| Model + details                                   | List URL                       | Show URL                     |
|---------------------------------------------------|--------------------------------| ---------------------------- |
| [User Profiles](/api/profiles)                 | `GET /v3/analyze/profiles`     | `GET v3/analyze/profiles/:id` |
| [Companies](/api/companies)                    | `GET /v3/analyze/companies`    | `GET v3/analyze/companies/:id` |
| [Segments](/api/segments)                      | `GET /v3/edit/segments`        | `GET /v3/edit/segments/:id`  |
| [Tours](/api/tours)                            | `GET /v3/edit/tours`           | `GET /v3/edit/tours/:id`     |
| [Tour Interactions](/api/tour-interactions)    | `GET /v3/analyze/interactions` | `GET /v3/analyze/interactions/:id` |
| [Microsurveys](/api/surveys)                   | `GET /v3/edit/surveys`         | `GET /v3/edit/surveys/:id`   |
| [Microsurvey Responses](/api/survey-responses) | `GET /v3/analyze/responses`    | -   |
| [Experience Deliveries](/api/deliveries)       | `GET /v3/edit/deliveries`      | `GET /v3/edit/delivery/:id`   |
| [Rate Limit Groups](/api/limit-groups)         | `GET /v3/edit/limit_groups`    | `GET v3/edit/limit_groups/:id` |
| [Alert Groups](/api/alert-groups)              | `GET /v3/edit/alert_groups`    | `GET v3/edit/alert_groups/:id` |
| [Launchers](/api/launchers)                    | `GET /v3/edit/launchers`       | `GET /v3/edit/launchers/:id` |
| [Tooltips](/api/tooltips)                      | `GET /v3/edit/tooltips`        | `GET /v3/edit/tooltips/:id`  |
| [Webhooks](/api/webhooks)                      | `GET /v3/edit/webhooks`        | `GET v3/edit/webhooks/:id` |
| [Domains](/api/urls)                           | `GET /v3/edit/urls`            | `GET /v3/edit/urls/:id`      |
| [Tags](/api/tags)                              | `GET /v3/edit/tags`            | `GET v3/edit/tags/:id` |
| [Data Properties](/api/properties)             | `GET /v3/edit/properties`      | `GET /v3/edit/properties/:id`      |
| [Data Imports](/api/imports)                   | `GET /v3/edit/imports`         | `GET /v3/edit/imports/:id`      |



## Ideas for how you can use this API

Chameleon's REST API is made to be adaptable to your own custom use cases. Nevertheless, there are some cases where you might want to use it:

- **[Import data](/api/imports)** (via CSV) on a one-time or recurring basis.
- Register a **[Webhook](/api/webhooks)** to receive realtime notifications of **[Microsurvey Responses](/api/survey-responses)**.
- Use the **[Microsurveys Endpoint](/api/surveys)** to download **[Microsurvey Response](/api/survey-responses)** data periodically.
- Get a list of all the User data being sent to Chameleon and manipulate it (delete, update, etc.) through the **[User Profiles Endpoint](/api/profiles)**.
- Use an **[Alert Group](/api/alert-groups)** to alert when Experiences aren't completed in a specific amount of time.
- Monitor the Experiences assigned to your **[Rate Limit Groups](/api/limit-groups)**.
- Trigger an Experience via REST API that will run on next page load via **[Experience Deliveries](/api/deliveries)**.


> If there is any use case where you'd like to use our API but you're not sure how, feel free to [Contact us](https://app.chameleon.io/help).
